"""Real-world dataset preprocessing utilities.

This package provides small CLIs to convert real datasets placed under
`data/raw/` into the metadata + folder layout expected by `learn_tool.settings.prepare_dataloader`.
"""
